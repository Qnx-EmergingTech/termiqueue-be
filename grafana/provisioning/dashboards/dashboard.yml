apiVersion: 1

providers:
  - name: termiqueue
    orgId: 1
    folder: "termiqueue"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 60
    options:
      path: /etc/grafana/provisioning/dashboards
