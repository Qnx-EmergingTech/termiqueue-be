global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "termiqueue-backend"
    static_configs:
      - targets: ["termiq-backend:8080"]
    metrics_path: "/metrics"
    scheme: http